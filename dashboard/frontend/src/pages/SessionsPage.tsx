export function SessionsPage() {
  return (
    <section className="panel placeholder-page">
      <h1>会话</h1>
      <p className="muted-note">这里将显示 occ 聚合后的 CLI session 索引。当前不做仿聊天界面。</p>
    </section>
  );
}
